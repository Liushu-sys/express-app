# 取件助手 · 部署与维护操作说明

## 应用信息

- **应用名**：取件助手
- **线上地址**（永久）：https://liushu-sys.github.io/express-app/
- **GitHub 仓库**：https://github.com/Liushu-sys/express-app
- **技术栈**：纯前端 PWA（HTML + CSS + JS + Service Worker）
- **数据存储**：浏览器 localStorage（本地持久化，换设备不同步）

---

## 部署流程回顾（GitHub Pages 方案）

### 1. 项目结构

```
express-app/
├── index.html              # 主页面
├── styles.css              # 样式
├── app.js                  # 核心逻辑（地点树分组、已取沉底、数据持久化）
├── sw.js                   # Service Worker（离线缓存）
├── manifest.webmanifest    # PWA 清单
├── icon.svg                # 矢量图标
├── icon-180.png            # iOS 触摸图标（必需）
├── icon-192.png            # Android 图标
├── icon-512.png            # 高清图标
├── icon-maskable.png       # Android 自适应图标
├── .nojekyll               # 告诉 GitHub Pages 不要用 Jekyll 处理
└── .gitignore             # 忽略规则
```

### 2. Git 初始化与推送

```bash
# 进入项目目录
cd /Users/liushujun/Desktop/express-app

# 初始化 git
git init
git config user.name "lisu"
git config user.email "lisu@github.com"
git add -A
git commit -m "feat: 取件助手 v1.0"

# 关联远程仓库（SSH 方式，国内 HTTPS 连不上 GitHub）
git remote add origin git@github.com:Liushu-sys/express-app.git
git push -u origin main
```

### 3. SSH 密钥配置（一次性）

```bash
# 生成密钥
ssh-keygen -t ed25519 -C "lisu@github" -f ~/.ssh/id_ed25519 -N ""

# 查看公钥（复制到 GitHub → Settings → SSH and GPG keys → New SSH key → Authentication Key）
cat ~/.ssh/id_ed25519.pub
```

### 4. 开启 GitHub Pages

- 打开仓库 Settings → Pages
- Source 选 `main` 分支 + `/ (root)` 目录
- Save，等 1-2 分钟
- 拿到永久域名：`https://liushu-sys.github.io/express-app/`

---

## 更新代码重新部署

改完代码后，三条命令推上去，GitHub Pages 自动更新：

```bash
cd /Users/liushujun/Desktop/express-app
git add -A && git commit -m "更新说明"
git push
```

> 注意：如果改了 `styles.css` 或 `app.js`，需要把 `index.html` 中的 `?v=1.0.0` 改成 `?v=1.0.1` 等新版本号，否则用户浏览器会用缓存的旧版。`sw.js` 里的 `VERSION` 变量也要同步改。

---

## iPhone 装到桌面

1. 用 **Safari** 打开 https://liushu-sys.github.io/express-app/
2. 点底部 **分享按钮**（方框+向上箭头）
3. 下滑找到 **添加到主屏幕**
4. 点右上角 **添加**
5. 桌面出现「取件助手」图标，点开就是全屏 App

> 必须用 Safari，微信/QQ 内置浏览器不能装桌面。

---

## 功能说明

- **添加快递**：点右下角 `+`，填快递名称、取货地点（用 `/` 分级，如「蜂巢柜/1号柜」）、取货码
- **标记已取**：点条目左侧小圆点，自动沉到底部「已取」列表
- **恢复已取**：展开底部「已取」列表，点「恢复」按钮
- **删除**：点条目右侧 `×` 按钮（会弹撤销提示）
- **数据持久**：所有记录存在浏览器本地，关闭重开不丢

---

## 踩坑记录

1. **IGA Pages 预览链接只有 3 小时有效期**：`?iga_token=...&iga_time=...` 过期后 401，不能用于长期使用
2. **Vercel CLI 交互式选择在 Trae 终端里不工作**：上下键选择显示不全
3. **surge.sh 在 Trae 终端里 stdin 被吞**：交互式注册无法完成
4. **GitHub HTTPS 在国内连不上**：75 秒超时，需用 SSH 方式
5. **GitHub Pages 子路径部署 — 所有资源路径必须用相对路径**：`index.html` 和 `sw.js` 里的资源路径必须用 `./` 相对路径，不能用 `/` 绝对路径。否则 GitHub Pages 子路径（如 `/express-app/`）下所有 CSS/JS/图标都会 404
6. **.nojekyll 文件**：GitHub Pages 默认用 Jekyll 处理，会忽略 `_` 开头的文件；加一个空 `.nojekyll` 文件可以禁用
7. **Trae 沙箱权限**：Agent 的 Shell 无法写 `~/.iga/`、`~/.gitconfig` 等家目录文件，需用 `git config`（项目级）而非 `git config --global`
8. **PWA 添加到主屏幕后 404（关键修复）**：`manifest.webmanifest` 里的 `start_url` 和 `scope` 如果写成 `/`，iOS 从桌面图标打开时会跳到 `https://liushu-sys.github.io/`（根域名），而不是 `/express-app/`（子路径），导致 404。**必须改成 `./`（相对路径）**。同样，manifest 里的 icon `src` 也要从 `/icon-192.png` 改成 `./icon-192.png`。Service Worker 的注册路径也要从 `register('/sw.js')` 改成 `register('./sw.js')`。
9. **更新代码后需要改版本号**：修改了 `styles.css` 或 `app.js` 后，必须把 `index.html` 中的 `?v=1.0.x` 改成新版本号，同时 `sw.js` 里的 `VERSION` 变量也要同步改，否则用户浏览器会使用缓存的旧版（Service Worker 缓存陷阱）
